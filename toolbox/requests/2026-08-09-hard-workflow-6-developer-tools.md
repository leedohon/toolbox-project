# 하드 워크플로 5회차 — 개발 도구 마무리

- 실행 ID: `20260809-150219-hard`
- 예약 종료: 없음
- 대상: 정규식, 해시, JWT, XML, URL, 웹 메타

## 확정 범위

- 정규식 테스트에 이메일·숫자·URL 예제를 빠르게 불러온다.
- 해시 결과를 체크섬 TXT 파일로 저장한다.
- JWT 헤더와 페이로드를 한 번에 복사한다.
- XML 변환 결과를 다시 입력으로 옮겨 이어서 편집한다.
- URL 쿼리 행을 이름순으로 정렬한다.
- 웹 메타 결과에서 공유 이미지 태그를 한 번에 제외한다.

## 효율 개선

- QA 기록 대상도 `--from-run`으로 실행 결과의 대상 목록을 재사용한다.
- 전체 토큰 계측값이 없으므로 대체 지표만 기록한다.
