import {mountGeneratedTool} from '../../assets/generated-tool-runtime.js?v=0.1.1';
mountGeneratedTool({
  "slug": "fraction-simplifier",
  "preset": "fraction-simplifier",
  "fields": [
    "numerator",
    "denominator"
  ]
});
