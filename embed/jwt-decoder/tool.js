import {copyText,setupEmbedHeight} from '../../assets/play-tools.js?v=0.3.2';
const $=s=>document.querySelector(s),tr=(ko,en)=>window.ToolboxI18n?.language==='en'?en:ko;let payloadText='';
function status(ko,en,error=false){const el=$('#jwt-status');el.textContent=tr(ko,en);el.className=`st-status ${error?'is-error':'is-good'}`;}
function decode(part){const normalized=part.replaceAll('-','+').replaceAll('_','/').padEnd(Math.ceil(part.length/4)*4,'='),bytes=Uint8Array.from(atob(normalized),character=>character.charCodeAt(0));return JSON.parse(new TextDecoder().decode(bytes));}
function run(){try{const token=$('#jwt-input').value.trim(),parts=token.split('.');if(parts.length!==3||parts.some(part=>!part))throw new Error(tr('점으로 구분된 세 구간 JWT를 입력해 주세요.','Enter a JWT with three dot-separated segments.'));const header=decode(parts[0]),payload=decode(parts[1]);payloadText=JSON.stringify(payload,null,2);$('#jwt-header').value=JSON.stringify(header,null,2);$('#jwt-payload').value=payloadText;const lines=[];for(const key of ['iat','nbf','exp'])if(Number.isFinite(payload[key])){const date=new Date(payload[key]*1000);lines.push(`${key}: ${Number.isNaN(date.valueOf())?tr('잘못된 시각','Invalid date'):date.toLocaleString(window.ToolboxI18n?.language==='en'?'en-US':'ko-KR')}`);}if(Number.isFinite(payload.exp))lines.push(payload.exp*1000<Date.now()?tr('현재 기준 만료된 값입니다.','The token value is expired at the current time.'):tr('현재 기준 만료 전 값입니다.','The token value is not expired at the current time.'));$('#jwt-times').textContent=lines.join(' · ')||tr('표시할 대표 시간 항목이 없습니다.','No standard time claims were found.');$('#jwt-result').hidden=false;status('내용을 열었습니다. 서명은 검증하지 않았습니다.','Contents decoded. Signature was not verified.');}catch(error){payloadText='';$('#jwt-result').hidden=true;status(`JWT를 확인해 주세요: ${error.message}`,`Check the JWT: ${error.message}`,true);}}
$('#jwt-run').addEventListener('click',run);$('#jwt-input').addEventListener('input',()=>{$('#jwt-result').hidden=true;payloadText='';});$('#jwt-copy').addEventListener('click',async()=>{if(!payloadText)return status('먼저 JWT 내용을 여세요.','Decode a JWT first.',true);if(await copyText(payloadText)){$('#jwt-fallback').hidden=true;status('페이로드 JSON을 복사했습니다.','Payload JSON copied.');}else{$('#jwt-fallback').value=payloadText;$('#jwt-fallback').hidden=false;}});$('#jwt-reset').addEventListener('click',()=>{$('#jwt-input').value='';$('#jwt-result').hidden=true;$('#jwt-fallback').hidden=true;payloadText='';status('입력을 비웠습니다.','Input cleared.');});addEventListener('toolbox-language-change',()=>{if(payloadText)run();});setupEmbedHeight('jwt-decoder',{content:true});
