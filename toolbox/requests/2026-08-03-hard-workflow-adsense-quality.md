# 2026-08-03 하드 워크플로 및 AdSense 품질 대응

## 확정 요청

- 하드 워크플로를 실행한다.
- 하드 워크플로 완료 뒤 Google의 `가치가 별로 없는 콘텐츠` 및 `게시자 콘텐츠가 없는 화면의 광고` 안내에 맞춰 공개 범위를 전수 점검하고 필요한 보강을 이어서 처리한다.
- 예약 종료와 시스템 종료는 실행하지 않는다.
- AdSense 재심사 제출은 이번 작업 범위에 포함하지 않는다.

## 하드 워크플로 범위

### 공개 도구 개선 6건

1. `color-converter-contrast`: 통합 허브 로딩 상태와 저장소 차단 대응
2. `jwt-decoder`: 깨진 한글 UI 복구 및 시간 항목 해석 개선
3. `multipurpose-draw-game`: 선택한 게임만 지연 로드하고 저장소 차단 대응
4. `image-mosaic-tool`: 깨진 한글 UI 복구 및 KOR / ENG 상태 문구 정비
5. `image-resizer-compressor`: 통합 허브 로딩 상태와 저장소 차단 대응
6. `date-calculator`: 통합 허브 로딩 상태와 저장소 차단 대응

### 신규 공개 완료 3건

- `text-extractor-tool`
- `web-meta-preview-builder`
- `ip-network-calculator`

기존에 구현까지 끝났으나 공개하지 않은 후보 중, 사용 목적이 분명하고 브라우저 안에서 독립적으로 실용 가치를 제공하는 세 도구만 선택한다. 금융 참고값 오해 가능성이 있는 `loan-savings-calculator`와 상대적으로 우선순위가 낮은 `coordinate-distance-calculator`는 계속 비공개로 둔다.

### 공통 결함 2건

1. 브라우저 저장소 접근이 차단된 환경에서 통합 허브 초기화·전환이 중단되는 문제
2. 아직 불러오지 않은 iframe을 연 직후 완료 상태로 잘못 안내하거나 모든 하위 기능을 미리 불러오는 문제

## AdSense 품질 대응 기준

- 광고가 붙을 수 있는 공개 화면은 고유한 게시자 설명, 사용 예시, 결과 해석과 제한 사항, FAQ를 갖춘 완성된 도구 게시글로 제한한다.
- 준비 중·비공개 후보·은퇴 호환 화면은 공개 카탈로그와 검색 색인에서 제외한다.
- 홈, 이동·검색 전용 화면, 법적 고지, iframe 실행 화면에는 광고 코드를 직접 두지 않는다.
- 공개 게시글과 실행 도구의 모바일·데스크톱 사용 가능 여부를 검증한다.
- 자동 광고 비활성화의 공개 반영 상태는 확인하되, 별도 명령 없이 재심사를 제출하지 않는다.

## 검증 및 기록

- 영향 기반 자동 검사와 공개 URL의 데스크톱·모바일 화면 검증을 수행한다.
- Blogger 반영, Git 커밋·푸시, GitHub Pages 공개 확인까지 완료한다.
- 결과는 `toolbox/automation-results/unchecked/`에 `confirm: N`으로 저장한다.
- 실제 토큰 수가 제공되지 않으므로 개발 토큰 감소율은 `proxy_only`로 기록하고 확인 가능한 대체 지표만 남긴다.
